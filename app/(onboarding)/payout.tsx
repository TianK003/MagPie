import { Text } from 'react-native';

import { Screen } from '../../src/components/Screen';

// Placeholder — step 3 "Where's the money going?" is a later task.
export default function Payout() {
  return (
    <Screen>
      <Text className="font-grotesk-bold text-title tracking-heading text-ink">payout</Text>
    </Screen>
  );
}
