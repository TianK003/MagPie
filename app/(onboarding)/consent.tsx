import { Text } from 'react-native';

import { Screen } from '../../src/components/Screen';

// Placeholder — step 1 "The honest part" consent checklist is a later task.
export default function Consent() {
  return (
    <Screen>
      <Text className="font-grotesk-bold text-title tracking-heading text-ink">consent</Text>
    </Screen>
  );
}
