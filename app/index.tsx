import { Text, View } from 'react-native';

// Placeholder screen so the router boots. Real screens are built by later tasks.
export default function Index() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>magpie</Text>
    </View>
  );
}
