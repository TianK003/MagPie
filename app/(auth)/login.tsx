import { Text } from 'react-native';

import { Screen } from '../../src/components/Screen';

// Placeholder — real email-OTP login is a later task.
export default function Login() {
  return (
    <Screen>
      <Text className="font-grotesk-bold text-title tracking-heading text-ink">login</Text>
    </Screen>
  );
}
